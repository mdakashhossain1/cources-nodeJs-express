<nav class="glass-morphism p-4 mb-8">
    <div class="container mx-auto flex justify-between items-center">
        <a href="/" class="text-white text-2xl font-bold">CourseHub</a>
        <div class="space-x-4">
            <% if (typeof user !== 'undefined' && user) { %>
                <span class="text-white">Welcome, <%= user.username %></span>
                <a href="/courses" class="text-white hover:text-gray-200">Courses</a>
                <% if (user.role === 'admin') { %>
                    <a href="/admin/dashboard" class="text-white hover:text-gray-200">Admin Dashboard</a>
                <% } %>
                <a href="/auth/logout" class="text-white hover:text-gray-200">Logout</a>
            <% } else { %>
                <a href="/auth/login" class="text-white hover:text-gray-200">Login</a>
                <a href="/auth/register" class="text-white hover:text-gray-200">Register</a>
            <% } %>
        </div>
    </div>
</nav> 